services:
  - type: web
    name: youtube-downloader
    env: node
    buildCommand: npm install && npm run build:css
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /
    autoDeploy: true 